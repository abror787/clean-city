import 'package:flutter/material.dart';

class DriverTaskHistoryScreen extends StatelessWidget {
  const DriverTaskHistoryScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('История заданий'));
  }
}
