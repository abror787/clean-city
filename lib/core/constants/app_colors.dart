import 'package:flutter/material.dart';

class AppColors {
  // Primary
  static const primary = Color(0xFF2E7D32);      // Green
  static const secondary = Color(0xFF1976D2);    // Blue
  static const accent = Color(0xFFFFA726);       // Orange
  
  // Status
  static const reported = Color(0xFFFFC107);     // Yellow - New Report
  static const inProgress = Color(0xFF2196F3);   // Blue - In Progress
  static const cleaned = Color(0xFF4CAF50);      // Green - Completed
  static const reopened = Color(0xFFF44336);     // Red - Reopened
  
  // Neutral
  static const background = Color(0xFFFAFAFA);
  static const surface = Color(0xFFFFFFFF);
  static const textPrimary = Color(0xFF212121);
  static const textSecondary = Color(0xFF757575);
  static const border = Color(0xFFE0E0E0);
  
  // Additional
  static const error = Color(0xFFD32F2F);
  static const success = Color(0xFF388E3C);
}
